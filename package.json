{
  "name": "hostelmate",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && cp -r dist public || true",
    "install": "cd backend && npm install"
  }
}
